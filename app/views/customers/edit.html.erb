<h1>Editing Customer</h1>
<%= render 'form' %>

<%= link_to 'Show', location_department_path(@location, @employee, @customer), :class => 'btn btn-outline-info' %>
<%= link_to 'Back', location_departments_path(@location, @employee), :class => 'btn btn-outline-info' %>
